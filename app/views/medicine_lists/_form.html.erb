<%= form_with(model: medicine_list) do |form| %>
  <% if medicine_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medicine_list.errors.count, "error") %> prohibited this medicine_list from being saved:</h2>

      <ul>
        <% medicine_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :薬品名, style: "display: block" %>
    <%= form.text_field :medicineName %>
  </div>

  <div>
    <%= form.label :間隔, style: "display: block" %>
    <%= form.time_field :interval %>
  </div>

  <div>
    <%= form.label :メモ, style: "display: block" %>
    <%= form.textarea :memo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
